//! The [`Blocker`] taxonomy is defined in [`sbol_core::validation`]; this
//! module re-exports it so `crate::validation::blocker`/`Blocker` paths and the
//! generated rule catalog are unchanged.

pub use sbol_core::validation::Blocker;
