# sbol3-12901: Workflow stage role recommendation

**Status:** Deferred  •  **Spec section:** §A.1.1  •  **PolicyOptions knob:** `(none; coverage-only)`

## Spec wording

> A prov:Activity with a Table 20 type SHOULD NOT use Usage roles other than same-stage or prior-stage roles.

## Ambiguity

SHOULD-rule with Table 20/21 cross-reference; no closed algorithm.

## Decision

Coverage records `Deferred(Policy)`. No knob.

## Rationale

Workflow recommendations are lab-process scoped; out of scope for a local validator.

## Examples

### Conservative (default)
Matches today's validator behavior: the diagnostic shape and severity are exactly what the current test suite expects.

### Strict
(promotes the warning to an error, or activates a check that is currently suppressed.)

### Lenient
(drops the check; the rule is recorded in coverage as suppressed.)
