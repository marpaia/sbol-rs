# sbol3-13401: om:Measure type recommendation

**Status:** Deferred  •  **Spec section:** §A.2.1  •  **PolicyOptions knob:** `(none; coverage-only)`

## Spec wording

> An om:Measure's type SHOULD identify a quantity-kind via the OM ontology.

## Ambiguity

SHOULD-rule. OM quantity-kind taxonomy is not bundled.

## Decision

Coverage records `Deferred(Policy)`. No knob until OM coverage expands.

## Rationale

Cross-cuts ontology and policy; defer.

## Examples

### Conservative (default)
Matches today's validator behavior: the diagnostic shape and severity are exactly what the current test suite expects.

### Strict
(promotes the warning to an error, or activates a check that is currently suppressed.)

### Lenient
(drops the check; the rule is recorded in coverage as suppressed.)
